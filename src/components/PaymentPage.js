import React from "react";
import { useLocation } from "react-router-dom";
const PaymentPage = () => {
  const location = useLocation();

  return <div>PaymentPags</div>;
};

export default PaymentPage;
